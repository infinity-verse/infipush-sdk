<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network permissions for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Required for Android 13+ notification display -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Wake lock for reliable delivery on older APIs -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application>
        <!--
            InfiPushMessagingService extends FirebaseMessagingService.
            This entry lets the SDK intercept FCM messages even when the app is in background.
        -->
        <service
            android:name=".service.InfiPushMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!--
            NotificationClickActivity handles click events from notifications
            and pings the backend tracking endpoint before opening the launch URL.
        -->
        <activity
            android:name=".receiver.NotificationClickActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:exported="false" />
    </application>

</manifest>
